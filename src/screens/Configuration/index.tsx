import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

const Configuration: React.FC = () => {
  return(
    <>
        <SafeAreaView>
            <Text>Configuration Template</Text>
        </SafeAreaView>
    </>
  );
}

export default Configuration;